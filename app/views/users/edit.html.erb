<h1>Edit account</h1>

<%= form_for(@user) do |f| %>
  <%= render 'fields', :form => f %>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<div>
  <%= user_gravatar @user %>
  <a href="http://gravatar.com/emails">change</a>
</div>

